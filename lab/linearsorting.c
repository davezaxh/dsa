// Implementing linear sorting

#include <stdio.h>

void main(){
    int a[10], i, n temp;

    printf("Enter the number of elements: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++){
        printf("Enter the element %d: ", (i+1));
        scanf("%d", &a[i]);
    }

    for(i = 0; i < n; i++){
        
    }
}